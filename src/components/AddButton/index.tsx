import { Button } from '../../styles'

const AddButton = () => {
  return (
    <>
      <Button to="/new">
        <span>Adicionar contato</span>
        <span>+</span>
      </Button>
    </>
  )
}

export default AddButton
